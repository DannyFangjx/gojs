

function run(input){
    const array = [1, 2, 3, 4, 5];
    if (array.includes(3)) {
        console.log('数组中包含 3');
        return 1
    } else {
        console.log('数组中不包含 3');
        return 0
    }
}
